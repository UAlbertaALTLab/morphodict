#ifndef settings_h
#define settings_h

/// This is how Swift code shared between all morphodict dictionaries knows which particular app to run
/// It's exported in an environment variable for Python to use
const char* morphodict_sssttt(void);

#endif /* settings_h */
