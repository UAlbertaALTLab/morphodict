def do_privileged_setup(args):
    """Perform privileged setup operations such as creating users

    This generally only needs to be run once when first setting up a machine to
    run morphodict from docker, or or if a new dictionary is being added.
    """
